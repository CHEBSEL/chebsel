import {AppState,updateState} from './app-state.js';
import {navigate} from './router.js';
import {saveAttendanceCompatible,readAttendanceEnvelope} from './storage.js';
const el=(tag,cls,text)=>{const n=document.createElement(tag);if(cls)n.className=cls;if(text!=null)n.textContent=text;return n};const norm=s=>String(s||'').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');const uid=()=>globalThis.crypto?.randomUUID?.()||`call_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;const fullName=m=>[m?.first,m?.last].filter(Boolean).join(' ').trim()||m?.name||'Sans nom';const today=()=>new Date().toISOString().slice(0,10);const DEFAULT_ACTIVITIES=['Répétition','Prestation 2e dimanche EBSEL','Concert','Réunion','Formation','Culte','Veillée','Autre'];
export const STATUS={'':{label:'— Non marqué —'},P:{label:'Présent'},RM:{label:'Retard motivé'},RNM:{label:'Retard non motivé'},AM:{label:'Absence motivée'},ANM:{label:'Absence non motivée'},ANMP:{label:'Absence non motivée — prestation'}};
export function normalizeAttendanceStatus(raw,isPerformance=false){const s=String(raw||'').trim().toUpperCase();if(['P','PRESENT','PRÉSENT'].includes(s))return'P';if(['RM','RETARD MOTIVE','RETARD MOTIVÉ','R','RETARD EXCUSE','RETARD EXCUSÉ'].includes(s))return'RM';if(['RNM','RETARD NON MOTIVE','RETARD NON MOTIVÉ'].includes(s))return'RNM';if(['AM','ABSENCE MOTIVEE','ABSENCE MOTIVÉE','A','ABSENCE EXCUSEE','ABSENCE EXCUSÉE'].includes(s))return'AM';if(['ANMP','ANM PRESTATION','ABSENCE NON MOTIVEE PRESTATION','ABSENCE NON MOTIVÉE PRESTATION'].includes(s))return'ANMP';if(['ANM','ABSENCE NON MOTIVEE','ABSENCE NON MOTIVÉE'].includes(s))return isPerformance?'ANMP':'ANM';return STATUS[s]?s:''}
const canEdit=()=>['president','secretary'].includes(AppState.role),activeMembers=()=> (AppState.data.members||[]).filter(m=>m.active!==false).sort((a,b)=>fullName(a).localeCompare(fullName(b),'fr')),calls=()=>Array.isArray(AppState.data.attendance)?AppState.data.attendance:[],settings=()=>readAttendanceEnvelope().settings||{},activityTypes=()=>{const c=settings().activityTypes;return Array.isArray(c)&&c.length?[...new Set(c.filter(Boolean))]:DEFAULT_ACTIVITIES},isPerformance=c=>{const s=norm(c?.activityType||c?.activity||'');return s.includes('prestation')||s.includes('concert')},selectedCall=()=>calls().find(c=>String(c.id)===String(AppState.ui?.selectedAttendanceCallId))||null;
function pageHead(t,s){const h=el('div','page-head split-head'),c=el('div');c.append(el('h1','',t),el('p','muted',s));h.append(c);return h}function field(l,i){const w=el('label','form-field');w.append(el('span','',l),i);return w}function input(type,name,value=''){const x=document.createElement('input');x.type=type;x.name=name;x.value=value||'';return x}function select(name,options,value=''){const x=document.createElement('select');x.name=name;for(const[v,l]of options){const o=document.createElement('option');o.value=v;o.textContent=l;x.append(o)}x.value=value||'';return x}function minutesBetween(s,a){if(!s||!a)return 0;const[sh,sm]=s.split(':').map(Number),[ah,am]=a.split(':').map(Number);return Math.max(0,(ah*60+am)-(sh*60+sm))}function callStats(c){let present=0,late=0,absent=0,total=0,lateMinutes=0;for(const r of Object.values(c?.records||{})){const s=normalizeAttendanceStatus(r.status,isPerformance(c));if(!s)continue;total++;if(s==='P')present++;else if(['RM','RNM'].includes(s)){late++;lateMinutes+=Number(r.minutesLate||0)}else absent++}return{present,late,absent,total,lateMinutes,avgLate:late?Math.round(lateMinutes/late):0,punctualityRate:total?Math.round(present/total*100):0}}
export function renderAttendancePage(){const existing=selectedCall(),editable=canEdit(),cfg=settings(),w=el('section','page'),head=pageHead(existing?'Modifier l’appel':'Fiche d’Appel','Présence et ponctualité — CHEBSEL v2'),actions=el('div','member-actions'),hist=el('button','btn secondary','Historique / Ponctualité');hist.onclick=()=>navigate('attendance-history');actions.append(hist);if(editable){const p=el('button','btn secondary','Paramètres');p.onclick=()=>navigate('attendance-settings');actions.append(p)}head.append(actions);w.append(head);const meta=el('div','panel attendance-meta'),date=input('date','date',existing?.date||today()),activity=select('activityType',activityTypes().map(x=>[x,x]),existing?.activityType||existing?.activity||cfg.defaultActivity||activityTypes()[0]),start=input('time','startTime',existing?.startTime||cfg.defaultStartTime||'18:00'),note=input('text','note',existing?.note||'');note.placeholder='Observation générale facultative';meta.append(field('Date',date),field("Type d’activité",activity),field("Heure de l’activité",start),field('Observation',note));w.append(meta);const summary=el('div','attendance-live-summary'),list=el('div','attendance-list'),records={};w.append(summary,list);for(const m of activeMembers()){const old=existing?.records?.[m.id]||{},row=el('article','attendance-row attendance-row-time'),who=el('div','attendance-person');who.append(el('strong','',fullName(m)),el('span','muted',`N° ${m.no||'—'}`));const st=select('status',Object.entries(STATUS).map(([k,v])=>[k,v.label]),normalizeAttendanceStatus(old.status,isPerformance(existing))),arrival=input('time','arrivalTime',old.arrivalTime||''),mins=input('number','minutesLate',String(old.minutesLate??minutesBetween(existing?.startTime,old.arrivalTime)));mins.readOnly=true;const obs=input('text','memberNote',old.note||'');obs.placeholder='Motif / observation';const recalc=()=>{const n=minutesBetween(start.value,arrival.value);mins.value=String(n);if(n>0&&st.value==='P')st.value='RM';if(!n&&['RM','RNM'].includes(st.value)&&arrival.value)st.value='P';updateLive()};arrival.onchange=recalc;start.onchange=recalc;st.onchange=()=>{if(st.value==='ANM'&&isPerformance({activityType:activity.value}))st.value='ANMP';if(st.value==='ANMP'&&!isPerformance({activityType:activity.value}))st.value='ANM';updateLive()};if(!editable){st.disabled=arrival.disabled=obs.disabled=true}records[m.id]={st,arrival,mins,obs};row.append(who,st,arrival,mins,obs);list.append(row)}function updateLive(){let p=0,l=0,a=0,u=0,min=0;for(const r of Object.values(records)){const s=normalizeAttendanceStatus(r.st.value,isPerformance({activityType:activity.value}));if(s==='P')p++;else if(['RM','RNM'].includes(s)){l++;min+=Number(r.mins.value||0)}else if(s)a++;else u++}summary.textContent=`Présents ${p} • Retards ${l} (${min} min) • Absents ${a} • Non marqués ${u}`}updateLive();if(editable){const bar=el('div','sticky-save-bar'),save=el('button','btn primary',existing?'Enregistrer les modifications':'Enregistrer l’appel');save.onclick=()=>{const rec={};for(const[mid,r]of Object.entries(records)){const s=normalizeAttendanceStatus(r.st.value,isPerformance({activityType:activity.value}));if(s)rec[mid]={...(existing?.records?.[mid]||{}),status:s,arrivalTime:r.arrival.value,minutesLate:Number(r.mins.value||0),note:r.obs.value.trim()}}const now=new Date().toISOString(),call={...(existing||{}),id:existing?.id||uid(),date:date.value,activityType:activity.value,activity:activity.value,startTime:start.value,note:note.value.trim(),records:rec,createdAt:existing?.createdAt||now,updatedAt:now},next=[...calls()],i=next.findIndex(x=>String(x.id)===String(call.id));if(i>=0)next[i]=call;else next.push(call);saveAttendanceCompatible(next);updateState('data',{...AppState.data,attendance:next});updateState('ui',{...AppState.ui,selectedAttendanceCallId:call.id});window.CHEBSEL?.refreshNotifications?.();save.textContent='Enregistré ✓'};const fresh=el('button','btn secondary','Nouvel appel');fresh.onclick=()=>{updateState('ui',{...AppState.ui,selectedAttendanceCallId:null});window.CHEBSEL?.rerender?.()};bar.append(save,fresh);w.append(bar)}return w}
function monthHealth(rows){const map=new Map();for(const c of rows){const m=String(c.date||'').slice(0,7);if(!m)continue;const s=callStats(c),v=map.get(m)||{month:m,total:0,present:0,late:0,lateMinutes:0};v.total+=s.total;v.present+=s.present;v.late+=s.late;v.lateMinutes+=s.lateMinutes;map.set(m,v)}return[...map.values()].sort((a,b)=>a.month.localeCompare(b.month)).slice(-6).map(v=>({...v,punctuality:v.total?Math.round(v.present/v.total*100):0,avgLate:v.late?Math.round(v.lateMinutes/v.late):0}))}
export function renderAttendanceHistory(){const w=el('section','page');w.append(pageHead('Historique & santé de la ponctualité','Résumé chiffré et évolution du groupe sur les 6 derniers mois.'));const tools=el('div','panel member-toolbar'),q=input('search','q',''),month=input('month','month','');q.placeholder='Rechercher activité ou date…';tools.append(q,month);w.append(tools);const health=el('div','panel punctuality-health'),summary=el('div','attendance-live-summary'),list=el('div','attendance-history-list');w.append(health,summary,list);const render=()=>{const needle=norm(q.value),mo=month.value,all=[...calls()],rows=all.filter(c=>(!mo||String(c.date||'').startsWith(mo))&&(!needle||norm(`${c.date} ${c.activityType||c.activity}`).includes(needle))).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')));let P=0,L=0,A=0,T=0,M=0;for(const c of rows){const s=callStats(c);P+=s.present;L+=s.late;A+=s.absent;T+=s.total;M+=s.lateMinutes}const punctual=T?Math.round(P/T*100):0,avg=L?Math.round(M/L):0;summary.textContent=`${rows.length} appel(s) • Présents ${P} • Retards ${L} • Absents ${A} • Ponctualité ${punctual}% • Retard moyen ${avg} min`;health.replaceChildren();const k=el('div','member-stats');[['Ponctualité',`${punctual}%`],['Présences',P],['Retards',L],['Absences',A],['Retard moyen',`${avg} min`]].forEach(([a,b])=>{const x=el('div','member-stat');x.append(el('b','',String(b)),el('span','',a));k.append(x)});health.append(k);const chart=el('div','health-chart');for(const m of monthHealth(all)){const col=el('div','health-col'),bar=el('div','health-bar');bar.style.height=`${Math.max(4,m.punctuality)}%`;col.append(el('b','',`${m.punctuality}%`),bar,el('span','',m.month.slice(5)));chart.append(col)}health.append(chart);list.replaceChildren();for(const c of rows){const s=callStats(c),row=el('article','panel history-call'),copy=el('div');copy.append(el('h3','',c.activityType||c.activity||'Activité'),el('p','muted',`${c.date||'—'} • ${c.startTime||'—'} • Ponctualité ${s.punctualityRate}% • Retard moyen ${s.avgLate} min • P ${s.present} / R ${s.late} / A ${s.absent}`));const open=el('button','btn secondary',canEdit()?'Ouvrir / modifier':'Consulter');open.onclick=()=>{updateState('ui',{...AppState.ui,selectedAttendanceCallId:c.id});navigate('attendance')};row.append(copy,open);list.append(row)}};q.oninput=render;month.onchange=render;render();return w}
export function renderAttendanceSettings(){if(!canEdit())return el('div','panel','Accès réservé au Président et au Secrétaire.');const cfg=settings(),w=el('section','page');w.append(pageHead("Paramètres de l’appel","Types d’activité et heure par défaut."));const form=el('div','panel settings-form'),activity=select('defaultActivity',activityTypes().map(x=>[x,x]),cfg.defaultActivity||activityTypes()[0]),time=input('time','defaultStartTime',cfg.defaultStartTime||'18:00');form.append(field('Type d’activité par défaut',activity),field("Heure de l’activité par défaut",time));const types=el('div','panel');types.append(el('h3','',"Types d’activité"));const tags=el('div','member-actions');activityTypes().forEach(x=>tags.append(el('span','status-badge is-active',x)));const add=input('text','newActivity','');add.placeholder='Ajouter un type d’activité';const addBtn=el('button','btn secondary','Ajouter');addBtn.onclick=()=>{const v=add.value.trim();if(v){saveAttendanceCompatible(calls(),{...settings(),activityTypes:[...new Set([...activityTypes(),v])]});window.CHEBSEL?.rerender?.()}};const save=el('button','btn primary','Enregistrer les paramètres');save.onclick=()=>{saveAttendanceCompatible(calls(),{...settings(),defaultActivity:activity.value,defaultStartTime:time.value});save.textContent='Enregistré ✓'};types.append(tags,add,addBtn);w.append(form,types,save);return w}
