import {setState} from './app-state.js';import {loadSession,saveSession,clearSession} from './storage.js';let initialized=false;const VALID_ROLES=new Set(['president','secretary','treasurer','visitor']);export function restoreSession(){const session=loadSession();if(session&&VALID_ROLES.has(session.role)){setState({user:session.user||{name:session.name||'Utilisateur'},role:session.role});return session}setState({user:null,role:'visitor'});return null}export function signIn({role,user}){if(!VALID_ROLES.has(role))throw new Error('Rôle invalide');const session={role,user:user||{name:role},createdAt:new Date().toISOString()};saveSession(session);setState({role,user:session.user});return session}export function signOut(){clearSession();setState({user:null,role:'visitor'});location.reload()}export function initAuth(){if(initialized)return;initialized=true;restoreSession()}
